using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class GameManagerScript : MonoBehaviour
{
    //HUD
    //Health + Rage (mana) bars + labels
    // public Slider healthBar;
    // public Text healthLabel;
    // public Slider rageBar;
    // public Text rageLabel;
    // //Map + Quests window
    // public RenderTexture map;
    // public GameObject quests;
    // public Button pause;
    // public Button resume;
    // public Button main;

    // [Header("System Rules and Numbers")]
    // [Tooltip("Force of Gravity (Deefault -9.81)")] public float gravityForce = -9.81f;
    // [Tooltip("Max Wander Distance for NPCs")] public float wanderLimit = -30.0f;
    // [Tooltip("NPC Hearing Range")] public float hearingRange = 10.0f;
    // [Tooltip("NPC Panic Chance")] public float panicChance = 0.5f;

    // [Header("Player Stats")]
    // [Tooltip("Player Movement Speed")] public float playerSpeed = 2.0f;
    // [Tooltip("Base Player Health")] public int playerHealth = 50;
    // [Tooltip("Player Rage")] public int rageBase = 25;
    // public GameObject player;
    // Start is called before the first frame update
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
